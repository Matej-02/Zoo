# Pocket Zoo Ultimate

## Run locally
Open `index.html` in a modern browser.

## Update GitHub Pages
Replace the current repository `index.html` with this new `index.html`, commit the change, then refresh the deployed site with Ctrl+F5.

The game attempts to import saves from the earlier Pocket Zoo Deluxe version automatically. The new save key is separate, so the old browser save remains available.
